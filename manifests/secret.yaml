# FAKE teaching value only. Never put real credentials in this file.
apiVersion: v1
kind: Secret

metadata:
  name: secure-web-secret
  namespace: secure-app

type: Opaque

stringData:
  DEMO_API_KEY: "EXAMPLE_ONLY_NOT_A_REAL_SECRET"
